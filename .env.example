# AI UI Builder - Environment Configuration

# AI Mode: enable optional LLM-based vision processing
# Values: "on" or "off" (default: "off")
# When "off": Uses deterministic stub for stable, predictable behavior
# When "on": Attempts OpenAI vision analysis with fallback to stub
AI_MODE=off

# OpenAI API Key: Required only if AI_MODE=on
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here
